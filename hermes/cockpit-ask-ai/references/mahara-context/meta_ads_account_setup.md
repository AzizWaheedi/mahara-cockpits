---
name: meta_ads
description: meta ads integration for the Team's Account account. Use when the task requires meta ads for this account or label.
---

## meta ads

User given label: **Team's Account**

Connected by U09305KE2KS.

## ⚠️ Verify setup before activating

- **Budgets are integer minor-units** (cents/pence), no decimal point: £70.00/day = `7000`. NEVER multiply a value that is already in minor units by 100. Applies to `daily_budget` / `lifetime_budget` / `bid_amount` / spend caps.
- **Read the setup back from Meta and reconcile against the intended plan BEFORE activating** anything:
  - *Inventory:* compare the live campaign / ad-set count and names to what you intended. Mismatch means stop — do NOT activate (e.g. intended 8 ad sets but Meta shows 24).
  - *Budgets:* confirm each read-back budget equals the intended amount.
- **Launch scripts must be IDEMPOTENT:** look up existing objects by name first and skip/update them instead of blindly re-creating, so a re-run never duplicates ad sets.

## Page engagement (ad comments)

When a user asks to read, reply to, or hide comments on an ad:

1. Start with read-only discovery (`get_pages`, `list_ad_accounts`, `list_ads`). Match named Pages, ads, and comments by readable values — never by list position.
2. If more than one viable Page, ad, or comment remains, present short human-readable choices (name, Page/ad context, comment excerpt/author/time) and STOP — end the turn and wait for the user's answer. Never ask "which one?" and then read or act on all of them anyway: that makes the question pointless and shows data they did not ask for.
3. Keep Meta object IDs internal. Never ask the user for Graph API terms, endpoints, tokens, or object IDs Viktor can discover.
4. After resolving the ad, call `get_ad_comments`, then use the returned Page/comment IDs for reply or hide.
5. To reply or hide, CALL the tool. Both are approval-gated, so the approval request is raised for you — name the Page, ad, comment excerpt/author and the exact reply text in it. Do NOT ask "shall I post this?" in chat before calling: that double-prompts the user and leaves the action unexecuted.
6. Do not claim an action completed until the tool response confirms it. Speak in product language: Page, ad, comment, connection, or permission.

## Page posts

When a user asks to create, edit, or delete a visible Facebook Page post:

1. Discover the Page with `get_pages`. Match by readable name, never by list position.
2. Call `create_page_post`, `update_page_post`, or `delete_page_post`. These writes are approval-gated, so the approval request is raised for you — name the Page and the exact post text in it. Do NOT ask "shall I post this?" in chat before calling.
3. Do not use `create_ad_creative` or an unpublished ad creative. That is not a visible Page post.
4. Keep post IDs internal. Reuse the id returned by `create_page_post`. Never ask the user for a Graph object ID.
5. Do not claim the post was published, updated, or deleted until the tool response confirms it.



<!-- ══════════════════════════════════════════════════════════════════════════
     END OF AUTOGENERATED CONTENT - DO NOT EDIT ABOVE THIS LINE
     Your customizations below will persist across SDK regenerations.
     ══════════════════════════════════════════════════════════════════════════ -->

## Account Structure

Mahara Media agency account managing 17 client ad accounts across multiple businesses. All accounts active (status 1). SDK module: `sdk.tools.mcp_meta_ads`.

### Pages (with full permissions)

| Page | ID | Category | Instagram |
|------|----|----------|-----------|
| MaharaMedia | `587094101153861` | Marketing agency | `17841473441237528` (@mahara_media) |
| Olivar Design | `1125105954024321` | Engineering service | — |
| Mass Design | `664243990104297` | Interior design studio | — |
| RM Design | `530067406857981` | Interior design studio | — |

## Key IDs and References

| Ad Account | ID | Currency | Timezone | Business |
|------------|----|----------|----------|----------|
| MaharaMedia | `act_746108264865897` | USD | America/Los_Angeles | MaharaMedia (`767701513092162`) |
| Ahemco | `act_1746894405978812` | SAR | Asia/Beirut | MiriamMahara (`4040284602901460`) |
| Elitexe.sa | `act_1767180407640433` | USD | Asia/Riyadh | elitexse.sa (`1687431365781274`) |
| RM Designs | `act_985366551096162` | USD | Asia/Riyadh | RM Design (`530072650190790`) |
| Life Depth | `act_2378944772609634` | USD | Asia/Riyadh | Life Depth Construction (`924148023830655`) |
| Mofage | `act_1298343058123107` | QAR | Asia/Qatar | Mofage (`1172574171026256`) |
| Ideal Elite | `act_1009665871644699` | USD | Asia/Riyadh | AlNakhaba Real Estate (`564709043335576`) |
| Specific Dimension | `act_2181914702337687` | USD | Asia/Riyadh | MiriamMahara |
| Art Vision | `act_750052678056652` | USD | Asia/Kuwait | Art vision co (`2021182108803554`) |
| artist..airbrush | `act_1988430024784828` | SAR | Asia/Riyadh | artist..airbrush (`928947110609914`) |
| NGCC Kuwait | `act_2562475254167609` | USD | Etc/GMT+3 | MiriamMahara |
| Rising of Najd | `act_1326793265986543` | USD | Asia/Riyadh | Rising of Najd (`3863301177304249`) |
| Olivar Design | `act_1923309945035375` | USD | Asia/Riyadh | Olivar Designs (`497586462552251`) |
| Mass Design | `act_37532169989703452` | USD | Asia/Riyadh | massdesign_sa (`1419260855744252`) |
| MofaGE Mahara | `act_1790433979003370` | USD | Asia/Qatar | Mofage |
| Bayt Al imarah | `act_971153818910775` | USD | Asia/Kuwait | Bayt AL3marh (`707697207991041`) |
| AIVE Designs | `act_1910040802988539` | AED | Asia/Dubai | AIVE Designs (`1142533378943232`) |

**Currency notes:** Most accounts use USD. Exceptions: Ahemco & artist..airbrush (SAR), Mofage (QAR), AIVE Designs (AED). Min daily budget: 100 minor-units for USD, ~370 for SAR/QAR/AED.

## Important Functions

All write operations produce a **draft** — request user approval via Slack, then call `submit_draft(draft_id)`.

### Read Operations

| Function | Key args | Use for |
|----------|----------|---------|
| `list_ad_accounts()` | — | Enumerate all accessible accounts |
| `get_ad_account(ad_account_id)` | act ID | Account details, currency, status |
| `get_account_spend(ad_account_id, date_preset)` | `last_7d`, `last_30d` etc. | Quick spend summary |
| `list_campaigns(ad_account_id, limit, effective_status_filter)` | filter `["ACTIVE"]` | Campaign inventory |
| `get_campaign_summary(ad_account_id, date_preset)` | `last_30d` | Per-campaign performance table |
| `get_insights(object_id, level, fields, time_range, breakdowns)` | any object ID | Flexible performance data |
| `get_async_insights(ad_account_id, level, fields, ...)` | for large reports | Async report, poll with `get_async_report_status` then `get_async_report_results` |
| `list_ad_sets(ad_account_id, campaign_id)` | — | Ad sets for account or campaign |
| `list_ads(ad_account_id, ad_set_id, campaign_id)` | — | Ads list |
| `list_custom_audiences(ad_account_id)` | — | Audience inventory |
| `list_ad_rules(ad_account_id)` | — | Automated rules |
| `search_targeting(ad_account_id, targeting_type, query)` | types: `adinterest`, `adgeolocation` etc. | Find targeting options |
| `get_reach_estimate(ad_account_id, targeting_spec)` | — | Audience size estimate |
| `get_pages()` / `get_instagram_accounts()` | — | Connected pages & IG accounts |

### Write Operations (all produce drafts)

| Function | Use for |
|----------|---------|
| `create_campaign` | New campaign (always create PAUSED) |
| `create_ad_set` | New ad set with targeting, budget, optimization |
| `create_ad` | New ad linking creative to ad set |
| `create_ad_creative` | Creative with image/video/carousel via `object_story_spec` |
| `update_campaign` / `update_ad_set` / `update_ad` | Modify status, budget, targeting, etc. |
| `delete_campaign` / `delete_ad_set` / `delete_ad` | Remove objects |
| `duplicate_campaign` / `duplicate_ad_set` | Clone with optional rename |
| `upload_ad_image` / `upload_ad_video` | Media upload |
| `create_custom_audience` / `create_lookalike_audience` | Audience creation |
| `create_ad_rule` / `update_ad_rule` | Automated rules |
| `send_server_event` | CAPI events |

### Permission Notes

- `list_pixels` and some measurement endpoints require `ads_management` permission that some client accounts haven't granted — expect 403 on those. Use `get_insights` with pixel-related fields as a workaround.
- All pages have full task permissions (ADVERTISE, ANALYZE, CREATE_CONTENT, MESSAGING, MODERATE, MANAGE).

### Toolkit Skills

The `skills/collections/meta-ads-toolkit/` collection has 28 specialized skills for analysis, auditing, and campaign management. Read the collection index to find the right skill for any Meta Ads task.

---
*Last verified: 2025-06-28*
